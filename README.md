Check the deploy.sh script. You will need a RHEL/CentOS/Rocky compatible machine with a /perfanal directory on it.
The main prerequisite is that you should be able to passwordlessly SSH as root to that Linux machine.
Change the IP address in the variables of the deploy.sh script. The target directory can also be changed.
